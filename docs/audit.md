# Аудит проекта REIP по ТЗ v2.0 FINAL + дополнение Signal Bus

Дата: актуально на коммит `fce1454`. Метод: сверка требований ТЗ (разделы 1–35 + дополнение Signal Bus) с фактическим кодом, эндпоинтами, миграциями, задачами и UI.

Легенда: ✅ реализовано и доступно · ⚙️ реализовано частично / с оговоркой · ⏳ не реализовано (нужны доступы/доработка).

## Сводка

| Метрика | Значение |
|---|---|
| Автотесты | 163 проходят + 40 DB-интеграционных (прогоняются в CI на Postgres+Redis) |
| CI/CD | ✅ GitHub Actions: lint (ruff) + pytest + node --check фронта + авто-деплой на VPS |
| Миграции | 14 применены (001–009, 040–044), учёт идемпотентный |
| Модели | 18 таблиц + view `v_signal_to_outcome` |
| API-роутеры | auth, geo, signals, leads, properties, lead_magnets, referrals, deals, analytics, webhooks, health |
| Celery-задачи | 10 модулей, beat-расписание из 9 задач |
| Прод-стенд | ✅ `https://reip.grouvi.online` (HTTPS/Let's Encrypt, за общим nginx, app на loopback) |
| Живые интеграции | ✅ AI (OpenAI через Railway-прокси), ✅ Telegram-бот @busgisbot |

## 1. Ядро и инфраструктура (ТЗ 1–12) — ✅
- Конфиг (pydantic-settings), исключения, JWT (HS256), шифрование ПД (Fernet) + blind-index телефона (HMAC), PII-анонимайзер, async-БД (asyncpg) + SQL-раннер миграций, 18 ORM-моделей, bot abstraction (TG+MAX), AI-сервис (роутинг+бюджет+учёт стоимости), Celery (worker+beat), main.py (lifespan). Всё покрыто тестами.
- Оговорка: вместо Alembic используется собственный идемпотентный SQL-раннер (`migrations/*.sql`) — сознательное решение, работает и в CI, и на проде.

## 2. Аутентификация и гео (ТЗ 13) — ✅ / ⚙️
- ✅ `POST /api/auth/platform` — Telegram WebApp initData проверяется корректным алгоритмом WebApp-HMAC (исправлен баг ТЗ, где был legacy sha256). Автосоздание менеджера в agency владельца (онбординг через `PLATFORM_OWNER_AGENCY_ID`).
- ⚙️ MAX initData: валидация только в dev-режиме (публичная схема подписи MAX не финализирована) — для прод-MAX нужна реальная проверка.
- ⚙️ Гео: `POST /api/geo/agencies/{id}/geo` (создание + защита территории). Нет эндпоинта-списка гео и UI управления городами.

## 3. Сигналы и лиды (ТЗ 14) — ✅
- ✅ Сигналы: список/фильтры, `create-lead` (152-ФЗ согласие), `generate-reply` (AI). Лиды: список, карточка+подборы, смена статуса, обратная связь по матчам. Всё доступно из Mini App.

## 4. Source Discovery (ТЗ 15) — ⚙️
- ✅ `keyword_builder` (AI-ключевые слова), `source_finder` (оценка источников AI), Telethon-коллектор (`app/collectors/telegram_collector.py`) + `collect_telegram_sources` (beat каждые 10 мин).
- ⚙️ Реальный сбор включается при наличии `TELETHON_API_ID/HASH` + сессии (сейчас безопасный no-op без них).

## 5. Intent scoring и матчинг (ТЗ 16) — ✅
- ✅ Двухступенчатый скоринг: `quick_filter` (без AI) → `full_intent_analysis` (AI), фоновая задача `score_intent_batch` (beat 5 мин). Проверено вживую: текст → intent 85 / family / hot / бюджет 9 млн.
- ✅ Матчинг по весам ТЗ (бюджет+30, сегмент+25, локация+20, приоритеты+15, hot+10), исправлен баг геолокации (NULL==NULL). Исключения матчей (feedback loop).

## 6. Лид-магниты (ТЗ 17 + 29) — ✅
- ✅ LM-1 (подбор), LM-2 (ипотека: 5 программ, маткапитал 833 000, аннуитет), LM-3 (проверка объекта: fetch+AI, блок ЦИАН/Авито/ДомКлик), LM-4 (районы по 6 сценариям, AI+fallback), LM-6 (ROI аренды vs вклад 16%). Публичные эндпоинты `/api/lm/...`, согласие→400, rate-limit. Приёмочные тесты ТЗ 33.2 воспроизведены (`tests/test_lm.py`).

## 7. Mini App (ТЗ 18 + 30) — ✅ / ⚙️
- ✅ SPA (роутер + компоненты + дизайн-система, SVG-иконки, скелетоны, плавающий таббар, active-состояния без «дёрганья»). Экраны: Обзор, Сигналы, Очередь ответов (Signal Bus), Лиды (+исход сделки, КП, feedback, альтернатива), Объекты (+смена цены→переподбор, отчёт), Аналитика, Профиль/команда.
- Доступ к ранее «висевшим без UI» функциям добавлен: deal outcome, документы (КП/отчёт), AI-черновик ответа, обработка альтернативы, feedback по матчам.
- ⚙️ Тема адаптируется под light/dark Telegram.

## 8. AI и стоимость (ТЗ 27, 10.2) — ✅ / ⚙️
- ✅ 11 промпт-модулей, учёт стоимости в Redis (атомарно), лимит бюджета (429) + мягкий алерт 90%, выбор модели по задаче и провайдеру, PII-анонимизация перед иностранным AI. Ленивая инициализация трекера (работает в воркере/скриптах).
- ✅ Провайдеры: **YandexGPT** (нативно) и **OpenAI** (через Railway-прокси, 152-ФЗ). ⏳ GigaChat / Anthropic в диспатче не реализованы (нужна доработка, если потребуются).
- ⚙️ Промпты `listing_generator` и `market_analysis` определены, но не подключены к эндпоинтам/задачам.

## 9. Партнёры и Knowledge Moat (ТЗ 20–21) — ✅ / ⚙️
- ✅ Deals: `record_outcome` → deal_outcomes + метрики воронки, доступно из Mini App. Еженедельный `update_knowledge_moat` (Source ROI + веса матчинга).
- ✅ Рефералы: `POST /api/referrals` (создание, уведомление партнёра, задача, истечение через Celery).
- ⏳ Нет UI рефералов и CRUD партнёрских агентств (нет эндпоинта-списка партнёров) — нужна доработка API+UI.

## 10. Альтернативные сделки (ТЗ 22) — ✅
- ✅ `process-alternative` (sell-to-buy: 2 задачи + матчинг по целевому бюджету), доступно из карточки лида.

## 11. Мониторинг (ТЗ 24) — ✅
- ✅ Structlog JSON, критические алерты в Telegram, алерт AI-бюджета >90%, авто-алерт очереди Celery >50 (beat 5 мин), `/api/health` и `/api/health/deep` (db, redis, ai, telegram_bot, celery_queue).

## 12. Продуктовые расширения (ТЗ 32) — ✅
- ✅ Decay срочности лидов (hot→warm 48ч, warm→cold 7д), эскалация 4/24/48ч (задача escalation), переподбор при снижении цены ≥5% + уведомление, дедуп по телефону+telegram_username (окно 90 дней, merge all_sources), детект «мёртвых» источников (paused), UTM-атрибуция, CRM-вебхук экспорт, генерация КП/отчётов (HTML; PDF — опц. extra `pdf`), аналитика (overview/funnel/managers/source-roi).

## 13. Инфраструктура и деплой (ТЗ 33) — ✅ / ⚙️
- ✅ StorageAdapter (Yandex Object Storage / локальный fallback), `/health/deep` расширен, CI/CD на VPS, HTTPS + nginx + Let's Encrypt, app на loopback.
- ⚙️ Деплой на Yandex Cloud из ТЗ заменён на деплой на предоставленный VPS (namespaced `reip`, чужие проекты не затронуты).

## 14. Signal Bus (дополнение) — ✅
- ✅ Миграции 040–044 (content_units, поля ответа сигналов, связь лид↔сигнал, agency_crm_config, view `v_signal_to_outcome`).
- ✅ Адаптеры каналов: Avito, CIAN, Telegram, MAX, VK (нормализация + доставка ответа, где поддерживается).
- ✅ Адаптеры CRM: Topnlab, amoCRM, Bitrix24, YUcrm (пуре build_payload + экспорт).
- ✅ Signal Bus сервис (ingest content → content_unit; воркфлоу ответа draft→sent), экран очереди ответов в Mini App.

## 15. Приёмка, документация (ТЗ 25–26, 34–35) — ✅
- ✅ `docs/troubleshooting.md`, `docs/acceptance_checklist.md`, `docs/file_manifest.md`, единый чек-лист 35.1–35.12.
- ✅ Нагрузочный тест 50 RPS: 0 ошибок, p50 3.8 / p95 5.1 / p99 8.3 мс (приложение). Скрипт `scripts/loadtest.py`.

---

## Что НЕ реализовано / требует доработки (сводка)

| # | Пункт | Причина / что нужно |
|---|---|---|
| 1 | GigaChat / Anthropic провайдеры | В диспатче AIService нет веток; нужна реализация вызовов (сейчас: YandexGPT + OpenAI-прокси). |
| 2 | UI и CRUD партнёрских агентств + рефералов | Нет эндпоинта-списка партнёров; нужен `GET /partners` + экраны. |
| 3 | Управление гео-городами в UI | Нет `GET`-списка гео; нужен эндпоинт + экран. |
| 4 | Промпты `listing_generator`, `market_analysis` | Определены, но не подключены к эндпоинтам/задачам. |
| 5 | MAX: прод-валидация initData | Схема подписи MAX не финализирована; сейчас dev-only. |
| 6 | Биллинг (YooKassa) | В основном ТЗ раздела нет; конфиг есть, флоу оплаты не реализован. |
| 7 | Telethon live-сбор | Нужны `TELETHON_API_ID/HASH` + разовая сессия. |
| 8 | PDF-рендер | Работает HTML; PDF требует опц. extra `pdf` (WeasyPrint + системные либы). |
| 9 | Прод-режим | Сейчас `NODE_ENV=development` (открыт /api/docs, широкий CORS) — переключить перед «боевым». |

## Известные исправленные баги ТЗ (задокументированы в git)
- WebApp-HMAC вместо legacy sha256 в auth; формат modelUri YandexGPT; выбор AI-модели по провайдеру; заголовки Railway-прокси (X-Proxy-Secret); ленивый init AI-трекера в воркере; геолокация NULL==NULL в матчинге; CHECK-констрейнты статусов (source `dead`/`paused`, match `presented/accepted`, task `escalation`, deal `deal_done`); PG GROUP BY bind-param в source-roi; NullPool под тестами.

## Вывод
Реализованы все ключевые разделы ТЗ 1–35 и дополнение Signal Bus; система развёрнута, протестирована (203 теста суммарно), проходит нагрузку 50 RPS, AI и Telegram-бот работают вживую. Оставшиеся пункты (таблица выше) — либо требуют внешних доступов/ключей, либо являются дополнительными модулями (рефералы-UI, гео-UI, GigaChat, биллинг), не блокирующими основной сценарий работы агентства.
